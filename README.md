# anaxi..the way links are meant to be.
A jQuery plugin for styling links the easy, flexible and unique way. Uses no <code>!important</code> CSS rule, flexible and light.
# Dependencies
=> jQuery
=> ..... nothing else ;)
